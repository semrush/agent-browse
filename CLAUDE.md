This project contains a Claude Code skill for working with Stagehand (AI browser automation framework).

The instructions for semrush.net/semrush.com are stored in `instructions` folder. They are stored in a hierarchical structure to allow context building. See more in `context-resolver.ts`.